[tox]
envlist = py{36,37},py{36,37}-pdbpp
skip_missing_interpreters=true

[testenv]
deps =
    pytest
    pexpect
    py{36,37}-pdbpp: pdbpp
commands = pytest {envsitepackagesdir}/pytest_pdb_break.py
platform = linux

[pytest]
addopts = -p pytester --noconftest
norecursedirs = *
